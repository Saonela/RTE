{% extends 'base.html.twig' %}
{% block body %}
    <h1>{{ blogPost.name }}</h1>
    <h3>{{ blogPost.user.username }}</h3>
    <h2>{{ blogPost.date|date("Y-m-d") }}</h2>
    <h2>{{ blogPost.location}}</h2>
    <p>{{ blogPost.content }}</p>
{% endblock %}